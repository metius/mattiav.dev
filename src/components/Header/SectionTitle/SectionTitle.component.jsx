import * as React from "react";
import * as styles from './SectionTitle.module.scss';

const SectionTitle = () => {
  {/* Here I will need to create the component that determine the name based on the position on the page (Home, About, Projects, Contatcs) */}

  return(
    <div id={styles.sectionTitle}>Home</div>
  )
}
export default SectionTitle; 